export enum VALUE_AREA {
  HIGH = 'high',
  VAH = 'vah',
  POC = 'poc',
  EQ = 'eq',
  VAL = 'val',
  LOW = 'low'
}

export enum VALUE_AREA_PERIODS {
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly'
}

export enum VALUE_AREA_TENSE {
  PREVIOUS = 'previous',
  CURRENT = 'current'
}
