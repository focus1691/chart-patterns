/**
 * Enumerates possible positions of a price relative to a certain level or range.
 */
export enum PRICE_POSITION {
  INSIDE = 'inside',
  ABOVE = 'above',
  BELOW = 'below'
}

/**
 * Enumerates the types of zones used in technical analysis, typically identified as support or resistance levels.
 */
export enum LEVEL_ZONE {
  SUPPORT = 'support',
  RESISTANCE = 'resistance'
}
